CREATE TABLE EMP_LIKE AS SELECT * FROM EMP;


INSERT INTO EMP_LIKE(EMPNO, ENAME, JOB)
VALUES (9999, 'N%AME','NONE');

SELECT * FROM EMP_LIKE;

-- 첫글자 나오고 Escape뒤에 %자체를 문자 그대로 인식한다
SELECT * FROM EMP_LIKE WHERE ENAME LIKE '_\%%' ESCAPE '\';