CREATE TABLE EMP_DDL (
	EMPNO NUMBER(4),
	ENAME VARCHAR2(10),
	JOB VARCHAR2(9),
	MGR NUMBER(4),
	HIREDATE DATE,
	SAL NUMBER(7,2),
	COMM NUMBER(7,2),
	DEPTNO NUMBER(2)
); 

ALTER TABLE EMP_DDL ADD HP VARCHAR2(20);

ALTER TABLE EMP_DDL RENAME COLUMN HP TO CP;

ALTER TABLE EMP_DDL MODIFY EMPNO NUMBER(5);

ALTER TABLE EMP_DDL DROP COLUMN JOB;

RENAME EMP_DDL TO EMP_DDL2; 



TRUNCATE TABLE EMP_DDL2;

SELECT * FROM EMP_DDL2;

ROLLBACK;


DROP TABLE EMP_DDL2;
DROP TABLE DEPT_TEMP;
DROP TABLE DEPT_TEMP2;
DROP TABLE DEPT_TEMP3;
DROP TABLE EMP_LIKE;
DROP TABLE EMP_TEMP;
DROP TABLE EMP_TEMP2;
