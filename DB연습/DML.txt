CREATE TABLE DEPT_TEMP AS SELECT * FROM DEPT;

SELECT * FROM DEPT_TEMP;

DROP TABLE DEPT_JOIN;
DROP TABLE EMP_JOIN;

SELECT * FROM DEPT_TEMP;

INSERT INTO DEPT_TEMP (DEPTNO, DNAME, LOC)
	VALUES(50,'WEB','SEOUL');
	

INSERT INTO DEPT_TEMP VALUES(60,'DATABASE','INCHEON');

--ROLLBACK;
SELECT * FROM DEPT_TEMP;

INSERT INTO DEPT_TEMP VALUES(50, 'WEB','NULL');

INSERT INTO DEPT_TEMP VALUES(60, 'DATABASE',' ');

--ROLLBACK;
SELECT * FROM DEPT_TEMP;
INSERT INTO DEPT_TEMP (DEPTNO,DNAME)VALUES(50, 'WEB');

--ROLLBACK;


-- 구조만 복사
CREATE TABLE DEPT_TEMP2 AS SELECT * FROM DEPT WHERE 1 <> 1;

SELECT * FROM DEPT_TEMP2;


-- 테이블이 이미 있는 경우, 데이터만 일괄 추가
INSERT INTO DEPT_TEMP2 (SELECT * FROM DEPT);


CREATE TABLE EMP_TEMP AS SELECT * FROM EMP;

SELECT * FROM EMP_TEMP;

INSERT INTO EMP_TEMP(EMPNO, ENAME, JOB,HIREDATE)
VALUES(9998, '이이름','CLERK','2024-05-13'); --문자열 데이터 알아서 변환해줌

--ROLLBACK;

CREATE TABLE EMP_TEMP AS SELECT * FROM EMP;

SELECT * FROM EMP_TEMP;

INSERT INTO EMP_TEMP(EMPNO, ENAME, JOB,HIREDATE)
VALUES(9998, '이이름','CLERK',
	TO_DATE('2024//05.13','YYYY//MM.DD'));

INSERT INTO EMP_TEMP (EMPNO, ENAME, JOB, HIREDATE)
VALUES(9998, '이이름','CLERK',SYSDATE);


--ROLLBACK;

