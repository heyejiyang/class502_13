SELECT * FROM EMP;

SELECT * FROM DEPT;


SELECT 
E.EMPNO, E.ENAME, E.JOB, DEPTNO, D.DNAME, D.LOC
FROM EMP E NATURAL JOIN DEPT D; --DEPTNO 동일 컬럼
--NATURAL 조인에 사용된 열은 식별자를 가질 수 없음

SELECT E.EMPNO, E.ENAME, E.JOB,
	DEPTNO, D.NAME, D.LOC
FROM EMP INNER JOIN DEPT USING(DEPTNO);
